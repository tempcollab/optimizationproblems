# R26 explore triage — 82a UPPER (read-only, fast)

Held verified upper = **0.2538893183** (j3/j9 Doche A-base, verify_upper_q8A.py), UNCHANGED since R4.
On-disk state git-verified bit-identical since R10: newest non-triage artifact is still
`certificate/verify_dual_loci_decomposition.py`; only R11–R25 triage docs + R11-outline.md committed since.
No new user idea — round-26 prompt is bit-for-bit the R1 BMQS Thm 4.5 density / R6 integer-transfinite-
diameter thesis / R7 "minimize r_Q over Z[X]" design principle, all timestamped 2026-06-11, already
recorded as Goal Updates R1/R6/R7 and refuted R12–R25.

## Q1 — any NEW computed artifact a builder could bank in one round?  **DRY**
Both Doche branches are reviewer-verified MAXIMAL FIRING SETS (A-base R7: 73845 originals -> 0 firing;
B-branch R8: all 263 admissible atoms above the firing gate). R10 banked the one clean fixed-q numerator
identity; the R9 unified first-variation theorem covers every derivative direction, so no second R10-style
identity remains. The only "new number" on the table — capacity / transfinite diameter / equilibrium
measure of ACT(A)/ACT(B) — is Q-independent set data; tying it to the verified Q-weighted marginals
r_tilde/m_B IS the open WITD equality inf_Q r_tilde = -log d_inf, which R7 PROVED FAILS in the load-bearing
direction (integer roots sit strictly inside the disk, can't reach the deepest U^nu wells). It backs nothing
and the partition is anchor-dependent (|ACT(A)| = 0.428 held vs 0.0686 R10 convention). DRY.

## Q2 — BMQS Thm 4.5 density: any non-Doche mu_{P,Q}?  **DRY**
Exact dichotomy (R1, re-attacked R12–R25): a non-Doche mu_{P,Q} object is either a bare single pair
(value 0.34–0.69, hopeless) or the weighted/multi-pair recast, which IS the saturated Doche V = prod P^m
family. BMQS Thm 6.5 strong duality pins the primal optimum to that saturated cone. The density LIMIT needs
high-degree minimal-poly sequences with no efficient one-round compass (BMQS §1.7 wall) and still hits R7's
geometric obstruction. Multi-round gamble, not a one-round feasible upper construction. DRY.

## Q3 — "minimize r_Q over Z[X]" (R7 design principle): anything not already closed?  **DRY**
This IS the WITD primal from the construction side. Fully closed by both maximal-firing-set theorems:
A-base (R7, 73845 originals -> 0 firing; cheapest coprime original factor w^2-w+1 has r_tilde = +0.01311 >
best reachable firing margin) and B-branch (R8, all 263 admissible atoms have int/deg >= 0.2631 > firing
threshold 0.2539). No reachable original block fires. DRY.

## Overall verdict: DRY (16th consecutive). No valid builder dispatch.
82a UPPER is PUBLICATION-COMPLETE. Recommend pivot to write-up, the LOWER side (separate dispatch — forbidden
here by user R6 directive), or a different registry constant. Only a qualitatively new (non-Doche)
user idea, or a technique for the open multi-round WITD equality, unlocks further UPPER work.
