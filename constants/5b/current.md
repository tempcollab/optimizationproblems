# C_5b — current verified bottom line

Constant: C_5b = c* (Sidon density in (4,5)-sets), Erdős #757. We attack the UPPER bound.

## Status
none

## Bounds
table: 4/7 ≈ 0.5714286 (upper, [MT26]) · held: 4/7 (record reproduced — Lean-certified finite content + cited Thm 1.5 bridge; NOT improved)

Verified lower bound for context: 9/17 ≈ 0.5294 ([MT26]).

## Progress log
- R1: Verified Lean reproduction of the record. `lake build Constants.C5b` passes; the two finite, load-bearing facts about the 14-pt record gadget A_base — `Abase_weakSidon` (all 91 pair-sums distinct) and `Abase_hLe8` (all 2002 nine-subsets contain a 3-AP ⇒ h(A_base) ≤ 8) — `decide` with NO axiom dependence (kernel reduction only; no sorry, no native_decide). Witness `AbaseWitness8_no3AP` gives h ≥ 8, so h(A_base) = 8 and c* ≤ 8/14 = 4/7. The gadget→bound bridge ([MT26] Thm 1.5, c* = inf_n f(n)/n) is the single trusted-not-proved link, exposed as the explicit hypothesis `MTThm15`; `c5b_le_four_sevenths` rests only on it plus the 3 standard Mathlib axioms (propext, Classical.choice, Quot.sound). Independently re-derived all four finite facts in Python (combos count = 2002 confirmed in-kernel via #eval). This REPRODUCES the record; it does not beat it.
- R1: Verified obstruction window for the small-gadget upper-bound angle. From the PROVEN lower bound c* ≥ 9/17, every (4,5)-set of size N has h(A) ≥ ⌈9N/17⌉, so a size-N gadget beats 4/7 only if 7·⌈9N/17⌉ < 4N. Independently confirmed: N=14 and N=21 CANNOT beat 4/7 (floors 8 and 12 land exactly at 4/7). Smallest feasible N: 9, 11, 13, 15, 16, 17, 18, 20, 22, … Window-limited exact search found no beat at N=9,11 within the searched windows (suggestive, not a global non-existence proof).
- R2: VERIFIED DEFINITIONAL CORRECTION (soundness). The round-1 digest claimed (4,5)-set ≡ weak-Sidon (sums distinct). FALSE. Confirmed against arXiv:2602.23282 (paper definition: every 4-element subset has ≥5 distinct pairwise absolute differences) AND by exhaustive computation over all size-4..7 subsets of {0,…,17} (6762 (4,5)-sets, zero violations): **(4,5)-set ⟹ weak-Sidon, strictly** — {0,2,5,8,14,21,28} is weak-Sidon but NOT a (4,5)-set; {0,1,2,4} likewise. So the difference condition is strictly stronger. Consequence: the Lean engine's `MTThm15` is stated over `weakSidonB` (the WEAKER predicate), so it is unsound for future gadgets — a weak-Sidon non-(4,5)-set would falsely certify a bound. The round-1 4/7 bound SURVIVES: A_base is a genuine (4,5)-set (verified diffs45=True). Engine fix required before any new gadget: replace `weakSidonB` with a decidable `is45setB` (every 4-sublist has ≥5 distinct |x−y|, C(N,4) enumeration, decide-tractable). Lemma 2.3 (h via 3-AP independence number) remains valid since (4,5)⟹weak-Sidon.
- R2: VERIFIED negative result at N=18 (no beat) + strategic floor+1 finding. No 18-point (4,5)-set with h=10 (the only beating value at N=18) found across record-extension + SA + beam search; best is a genuine (4,5)-set with h=11 (ratio 11/18≈0.6111, a 1-swap local minimum). Verified arithmetic: the floor ⌈9N/17⌉ alone beats 4/7 for all feasible N≥9, but the empirically-reached value floor+1 first beats 4/7 only at **N=30** (17/30≈0.5667; N=28 floor+1=16/28 ties 4/7 exactly). Held stays 4/7. (Note: N15's separate claim of an "h=10 obstruction wall" at N=15 is NOT verified — the N18 record-extension exhibits a genuine 15-point (4,5)-set with h=9 = floor+1; neither value beats 4/7, so the bound is unaffected.)
- R3: VERIFIED ENGINE SOUNDNESS FIX (closes the R2-flagged gap; no bound move, held stays 4/7). `lean/Constants/C5b.lean` now keys the certificate to the correct (4,5)-set **difference** condition: new decidable `is45setB l` = every C(N,4) four-sublist has ≥5 distinct pairwise `|x−y|` (via `countDistinct`/`diffs6`); `MTThm15` antecedent and `c5b_le_four_sevenths` re-keyed from the strictly-weaker `weakSidonB` (sums) to `is45setB`. New `Abase_is45set : is45setB Abase = true` by kernel `decide` (C(14,4)=1001). Clean rebuild PASS; `#print axioms` clean (Abase_is45set: [propext]; c5b_le_four_sevenths: [propext, Classical.choice, Quot.sound]; no sorryAx/native_decide). Independently re-verified: A_base 0 violations / 1001 four-subsets (genuine (4,5)-set); separating set {0,2,5,8,14,21,28} correctly REJECTED by is45setB (#eval false) yet IS weak-Sidon (#eval true). The engine is now sound to certify the (4,5)-property of any future gadget; the R1 4/7 record survives on the correct definition.
- R3: VERIFIED TRANSVERSAL MACHINERY (reusable, no bound move). New `lean/Constants/C5bTransversal.lean`: `hLe_of_fracMatching` — coordinate-free LP-duality soundness lemma proving every no-listed-AP sublist S⊆A has |S|≤m from an O(|F|·N) fractional-matching certificate (edgesOK + loadOK D + D·(N−m−1) < totalW), in place of the C(N,m+1) enumeration. Fully proven (no sorry); clean rebuild PASS; axioms clean ([propext, Classical.choice, Quot.sound], no sorryAx/native_decide). Independently re-derived the double-counting core (for a hitting set H of the AP-hypergraph, totalW ≤ loadSum H ≤ D·|H|) — sound. Validated on A_base via `Fcert_base` (the 12 genuine 3-APs, D=6, T=27): `Abase_avoiders_le_9` fires end-to-end giving h(A_base)≤9. HONEST integrality gap confirmed independently (scipy): exact LP ν*=4.5, exact τ=6, so LP-duality gives only h≤9, NOT the tight h≤8 — the builder did NOT overclaim. Tight h≤8 needs an integral transversal cert (next sub-goal). No bound improvement; load-bearing prerequisite for an N≥21 h-certificate.
- R4: VERIFIED TIGHT BRANCHING TRANSVERSAL CERT (closes the R3 integrality gap; no bound move, held stays 4/7). New `lean/Constants/C5bBranch.lean`: `noTransLe : List edge → ℕ → Bool` is a structurally-recursive B&B hitting-set predicate (`noTransLe FW k = true` ⇔ τ(FW) > k); soundness lemma `noTransLe_sound` proven by induction on the budget k via the residual-hitting lemma `hitsAll_removeVertex_erase` (erasing the chosen branch vertex from a Nodup hitting set still hits every surviving edge); bridge `hLe_of_branchCert` reuses the C5bTransversal complement/cardinality split to give |S| ≤ N−k−1 for any no-listed-AP S⊆A. On A_base: `Abase_branch_tau : noTransLe Abase_APs 5 = true` (kernel decide, 364-node tree, axiom-free) reaches the INTEGRAL τ=6, so `Abase_avoiders_le_8` certifies the TIGHT h(A_base)≤8 — closing the R3 gap (fractional gave only ≤9). `lake build` PASS; axioms clean ([propext, Classical.choice, Quot.sound]; the two decide facts `Abase_branch_tau`/`Abase_APs_edgesOK` depend on NO axioms; no sorryAx/native_decide). Independently re-derived: the 12 listed APs = exactly the full 3-AP family of A_base; exact min-transversal τ=6 (witness {0,136,200,243,272,323}); noTransLe(5)=True, noTransLe(6)=False; bridge arithmetic 14−5−1=8. This re-derives the KNOWN record value via a tight scalable route (cost O(3^budget) not C(14,9)); it does not beat 4/7 — it removes the prerequisite blocker for certifying a future N≈30 gadget tightly.
- R4: VERIFIED HYBRID max-COMPOSITION LEMMA (machinery, no bound move, held stays 4/7). New `lean/Constants/C5bHybrid.lean`: `hLe_of_hybrid` composes the fractional (`hLe_of_fracMatching`, τ≥⌈ν*⌉) and branching (`hLe_of_branchCert`, τ≥k) lower bounds on the SAME min-transversal, giving |S| ≤ min(m_frac, N−k_branch−1) by a one-line `le_min` — trivially sound, adds no new trust. Validated on A_base (`Abase_avoiders_le_8_hybrid`): fractional ≤9, branch ≤8, min 9 8 = 8 (the max of τ-bounds prefers the branch τ=6), tight h≤8. `lake build` PASS; axioms clean ([propext, Classical.choice, Quot.sound]; no sorryAx/native_decide). HONEST SCOPE confirmed by reviewer (NO overclaim): this is the `max` composition only — it does NOT shrink the branch budget (A_base still certified at the full budget k=5=τ−1, not a small residual), so it does NOT yet deliver the N≈30 scaling the slug is named for. The genuinely-scaling interleaved cert (contract the fractional dual support, branch only the residual with budget τ−⌈ν*⌉) is the deferred further step.
- R5: VERIFIED INTERLEAVED (fix-then-branch) TRANSVERSAL LEMMA (machinery, no bound move, held stays 4/7). New `lean/Constants/C5bInterleaved.lean`: `hLe_of_interleaved` is the genuinely budget-shrinking version the R4 `max`-hybrid was not — an arbitrary hitting set H of the combined family is split as the COMPLEMENTARY-predicate partition `(H.filter P) ⊎ (H.filter ¬P)` (= H∩P ⊎ H\P), the fractional load lemma forces |H∩P|≥a (off-P vertices carry zero listed charge, requires `suppInP P FW_frac`) and the residual-hitting argument forces |H\P|≥g+1 (residual edges avoid P, requires `resAvoidsP P FW_res`, feeds `noTransLe_sound`), so |H|≥a+(g+1) branching ONLY the residual budget g, not the full τ. Load-bearing no-double-count is INSIDE the formalization: `List.length_eq_length_filter_add` makes the two parts a genuine partition (every vertex on exactly one side) and the two lower bounds attach to the two different sides; the disjointness conditions are required hypotheses, not assumed. `lake build` PASS; axioms clean ([propext, Classical.choice, Quot.sound]; no sorryAx/native_decide/ofReduceBool). Reviewer independently re-derived: the partition is structurally no-double-count; the synthetic A_syn validation (`Asyn_avoiders_le_8`) FAIRLY exhibits the budget-shrink end-to-end (a=2 fractional on 2 disjoint APs + g=1 branch on 2 disjoint residual APs ⇒ combined τ=4, branch tree 3^1=3 vs full 3^3=27) — the shrink the R4 max-hybrid could not do. HONEST FINDING confirmed by independent re-enumeration over all 2^14 supports P: A_base's 3-AP cover LP is fully half-integral (ν*=4.5, no integral/forced vertex), max over P of ⌈ν*(edges⊆P)⌉+τ(edges avoiding P) = 6 attained ONLY at P=∅ (a=0, full budget g=5) — so the dispatched g=1-on-A_base milestone is genuinely IMPOSSIBLE for this sound mechanism, not faked; the cert correctly degenerates to P=∅ on A_base (`Abase_avoiders_le_8_interleaved`, re-derives tight h≤8). The lemma is sound, reusable bound-mover machinery; to actually shrink the N≈30 budget it needs a candidate whose LP is NOT fully half-integral (an integral component). No bound improvement.
- R5: VERIFIED NEGATIVE exact-α gadget search at N∈{30,32,34} (no beat; held stays 4/7; nothing written to constants/5b.md). `constants/5b/certificate/search_N30.py`: exact-α (α=N−τ via scipy.milp, cross-checked by deterministic B&B; reviewer additionally re-derived τ with an independent from-scratch min-hitting-set B&B) search for ONE indecomposable (4,5)-set with h=⌈9N/17⌉+1. Best found is a (4,5)-valid Fibonacci-subsequence family, exact α=floor+3 at every N (N=30: α=19, ratio 19/30≈0.6333; reviewer independently confirmed is45set=True with min-distinct-diffs-over-quads = 5, indecomposable, and α=19 via independent B&B). τ-deficit of 2 at every N (best τ-density ≈0.367 vs needed ≈0.433, below even A_base's 0.4286). Search is heuristic so this is an honest obstruction map / conjecture, NOT an impossibility theorem; correctly scoped (no bound claimed, current.md untouched by the script).
- R8: VERIFIED LEAN CAP LEMMA `m ≤ n−2` (machinery, no bound move, held stays 4/7). New `lean/Constants/C5bCap.lean`: `m_le_n_sub_2 (hA : Is45 A) (hn : 2 ≤ A.card) : (threeAPs A).card ≤ A.card − 2`, hardening [MT26] Lemma 2.4 (previously a trusted external citation underpinning the whole cap/τ/Henning–Yeo analysis) into a `lake build`-checked theorem. Load-bearing FACT1 fully formalized: `diffs6_midpoint_repeats` shows a double-midpoint 4-subset {p−d₁,p−d₂,p+d₂,p+d₁} has `diffs6` entries [e0,e1,e2,e3,e1,e0] (entry 4=entry 1, entry 5=entry 0 by `ring`, ALL p,d₁,d₂), so `countDistinct ≤ 4` ⇒ `diffs4B = false` ⇒ violates is45setB's ≥5; hence `midpoint_degree_le_one` (no vertex is midpoint of two distinct 3-APs) and the injective midpoint map into the n−2 interior. `lake build Constants.C5bCap` PASS (full project 8559 jobs clean), Mathlib still v4.31.0. Axioms clean ([propext, Classical.choice, Quot.sound]; the two purely-arithmetic facts depend on [propext, Quot.sound] only; NO sorryAx/native_decide/ofReduceBool). REVIEWER INDEPENDENTLY VERIFIED: (a) re-derived FACT1 — max distinct |diffs| over all p,d₁>d₂>0 is exactly 4 (never ≥5); (b) confirmed the Lean diffs6 repeat structure (entry4==entry1, entry5==entry0) over 10⁴ random p,d₁,d₂; (c) `diffs4B` is permutation-invariant on distinct quadruples, so the abstract `Is45` (∀ orderings pass diffs4B) is FAITHFUL to the engine's `is45setB` (one combos-ordering per 4-subset) and NON-VACUOUS — the lemma genuinely constrains the (4,5) structure, not a mis-stated/vacuous proxy; (d) on A_base m=12=n−2 saturated, all 12 midpoints distinct (FACT1 holds), min/max not midpoints. The one honest scoping note (the decidable bridge `is45setB l = true → Is45 l.toFinset` not yet wired into the gadget path) is a legitimate next increment, NOT a fatal gap — the theorem is a genuine self-contained verified result. No bound improvement.
- R8: VERIFIED NEGATIVE max-m gadget search over a FIXED pool P (no beat; held stays 4/7; nothing written to constants/5b.md). `constants/5b/certificate/search_N30_stageA.py`: a bounded-compute Stage-A (maximize m = midpoint-distinct 3-AP count, hard wall-clock chunks, per-chunk progress printed) + Stage-B (exact α=N−τ via scipy.milp cross-checked by B&B, only on the high-m shell) over a fixed structured pool P (|P|=1461, window [0,2200]). RESULT: max m walls at 26 < cap n−2=28 over P; exact-α frontier m=26→α=20 (floor+4), m=25→α=21 (floor+5) — NO beat (a beat needs α≤17). REVIEWER INDEPENDENTLY VERIFIED both frontier sets: genuine (4,5)-sets (0 violations, min distinct |diffs|=5), m=26/25 confirmed, and α re-derived from scratch with a self-written min-transversal B&B = 20 and 21 (matching milp==bb exactly; transversals validated to hit every edge). Honestly scoped as a wall OVER THE NAMED POOL P, NOT a global impossibility — sharper than R5's blind-α wall (direct max-m objective, m=26 now reached inside a bounded window ≤2200 vs R5's ~2.18M-scale Fibonacci). Note: the local-search `stageA_best.json` set (α=22) is not the min-α set; the frontier correctly reports the min-α over the shell. No bound improvement.
- R8: VERIFIED STRUCTURAL SURVEY retiring the certifiability risk (R7 build, reviewed R8; no bound, held stays 4/7; constants/5b.md untouched). `constants/5b/certificate/halfint/lp_halfint_filter.py` (re-run exit 0, all asserts pass, ~90s): an LP-half-integrality filter over (4,5)-set candidates showing **A_base's fully-half-integral cover-LP pathology is NOT universal at N=30**. Headline: RAND30 (a genuine N=30 (4,5)-set) has an INTEGRAL cover LP (ν*=10=τ, all 30 x_v∈{0,1}) and is interleaved-cert-FRIENDLY at budget g=1. REVIEWER INDEPENDENTLY VERIFIED: RAND30 is a (4,5)-set (0 violations), 12 3-APs, exact τ=10 ⇒ α=20 (no beat); cover-LP optimum = 10.0 with zero fractional vertices (vs A_base's ν*=4.5); and the g=1 budget-shrink end-to-end — the 24-vertex support P contains 10 of the 12 edges with integral cover a(P)=8, residual 2 edges with τ(res)=2, so a(P)+τ(res)=8+2=10=τ ⇒ residual branch budget g=τ(res)−1=1 (vs full τ−1=9). The g=1 FRIENDLY verdict is correctly the one-sided constructive direction (tagged `[heuristic-P]`, not claimed as an exhaustive lower bound). This retires the R5/explorer-flagged risk that a found N≈30 beat could be uncertifiable by the interleaved cert. No bound improvement (RAND30 α=20).
