# 53a — Davenport constant of `C_n^3` — tracking file

`C_53 = sup_{n>=2} (D(C_n^3)-1)/(n-1)`. Movable side = upper bound.

## Status
none

## Bounds
table: `C_53 <= 4` (Grinsztajn 2026, from `D(C_n^3) <= 4n - P(n) - 2`) · held: `C_53 <= 4` (record; not yet independently re-derived unconditionally — the verified Lean artifact this round is a CONDITIONAL re-derivation of the recursion step only, not a new or stronger bound)

## Progress log
- R2: Lean scaffold verified (reviewer). `lake build Constants` green against pinned Lean v4.31.0 / Mathlib `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`; `#print axioms` on `recursion_step` and `comparison_ineq` = `[propext, Classical.choice, Quot.sound]` only (no `sorryAx`, no problem-specific axiom). Genuinely proved: the load-bearing comparison inequality `p^2-2p+2 <= (p-1)Q` (2<=p<=Q) and the integer recursion step (`D m <= 4m-Q-2`, `D(pm) <= p·D(m)+p^2`, `2<=p<=Q` ⊢ `D(pm) <= 4pm-Q-2`), both re-derived independently by the reviewer. Zero-sum number-theory inputs carried as explicit hypotheses, not axioms — honest conditional. INFRASTRUCTURE milestone; `held` UNCHANGED at 4, no bound improvement.
- R3: Global factorization induction verified (reviewer). `lake build Constants` green via genuine recompile ("Built Constants.C53a", not replayed); `#print axioms` on `global_induction` & `c53_le_4_per_n` = `[propext, Classical.choice, Quot.sound]`, `acc_cast_bridge` = `[propext]` — no `sorryAx`, no problem-specific axiom (source grep clean). Verified: the `List ℕ` fold of `recursion_step` over `base_in_form` correctly yields `D(C_n^3) <= 4n - P(n) - 2` for `n = Qn * ps.prod` — base case, cons step, and the ℕ/ℤ cast bridge (`acc_cast_bridge`) all genuinely discharged inside the formalization; algebra re-derived independently (0 violations over integer sweeps); hypotheses shown non-vacuous via a concrete witness `D m = 3m-2`. Zero-sum inputs (`hbase`, `hstep`) carried as explicit Lean hypotheses, not axioms — honest conditional. INFRASTRUCTURE milestone (conditional re-derivation of `C_53 <= 4`); `held` UNCHANGED at 4, no record-break. Carry-forward (faithfulness, not soundness): tie `ps` to `Nat.factors n` and lift `c53_le_4_per_n` to the `iSup` form.
- R4: Two faithfulness bridges verified (reviewer). `lake build Constants` green via genuine recompile ("Built Constants.C53a", not replayed); `#print axioms` on both new top-level theorems = `[propext, Classical.choice, Quot.sound]` only — no `sorryAx`, no problem-specific axiom (source grep clean: no `sorry`/`admit`/`axiom`/`native_decide` in code). (1) `factors_bridge_max` closes faithfulness-gap #1: ties `ps` to the ACTUAL `Nat.primeFactorsList n` (`Qmax = largest prime factor` via `perm_cons_erase`+`Perm.prod_eq`+`prod_primeFactorsList`), concluding `D n - 1 <= 4*((n:ℤ)-1)` read off the real factorization of `n`. (2) `c53_ratio_real_le` closes faithfulness-gap #2 per-`n`: packages the integer bound into the honest real ratio `((D n:ℝ)-1)/((n:ℝ)-1) <= 4` via `div_le_iff₀` (iSup honestly deferred — no overclaim). Both re-derived independently and shown non-vacuous (witness `D m=3m-2` at `n=12`: integer `33<=44`, ratio `3<=4`). HONESTY: `Q` is the largest PLAIN prime (Route L), `<= P(n)`; since the corollary needs only `2<=Q` the conclusion stays `<= 4(n-1)` — `C_53 <= 4` preserved, NOT a sub-4 claim. Zero-sum inputs (`hbase`,`hstep`) remain explicit Lean hypotheses — honest conditional. INFRASTRUCTURE milestones; `held` UNCHANGED at 4, no record-break.
- R5: iSup-over-ℝ capstone verified (reviewer). `lake build Constants` green via genuine recompile ("Built Constants.C53a (11s)", not replayed); `#print axioms` on all three new top-level theorems (`c53Ratio_le`, `c53_isup_real_le`, `C53_le_4`) = `[propext, Classical.choice, Quot.sound]` only — no `sorryAx`, no problem-specific axiom (source grep clean). Closes faithfulness-gap #2 fully: the per-`n` ℝ ratio (`c53_ratio_real_le`, R4) is packaged into the LITERAL supremum statement `C_53 = ⨆_{n≥2} (D(C_n^3)-1)/(n-1) ≤ 4` via a guarded total `c53Ratio D n = if 2≤n then ratio else 0` and `ciSup_le`. Decisive load-bearing fact independently re-verified: in pinned Mathlib `ciSup_le [Nonempty ι] (H : ∀ x, f x ≤ c) : iSup f ≤ c` has NO separate `BddAbove` obligation — the per-term bound IS the boundedness; reviewer re-derived the whole capstone from scratch (own `by_cases`/`if_pos`/`if_neg` split → green). Diff is 102 PURE INSERTIONS, zero upstream deletions — every R2–R4 theorem byte-identical, no weakening. `hbase` is now ∀-quantified over all `n≥2`, which is STRONGER (more demanding on `D`), not a vacuous weakening; anti-vacuity re-confirmed: witness `D m=3m-2` satisfies `hbase`(by `rfl`)/`hstep`(by `nlinarith [sq_nonneg (p-1)]`) and yields a genuine non-padded family (n=2 → ratio 3≤4), so `C53 Dwit ≤ 4` and `(⨆ n, c53Ratio Dwit n) ≤ 4` both build. 0-padding for n<2 (`0≤4`) cannot raise the sup — correct direction for an upper bound, no overclaim. Zero-sum inputs (`hbase`,`hstep`) remain explicit Lean hypotheses — honest conditional. INFRASTRUCTURE capstone (conditional re-derivation of `C_53 ≤ 4` now stated literally as a supremum); `held` UNCHANGED at 4, no record-break. Bounds table untouched (3|4).
