# current — Constant 3a (Gyarmati–Hennecart–Ruzsa sum-difference constant), LOWER bound

## Status
improved

## Bounds            table: 1.1740744 · held: 1.1781
Record to beat: C_3a > 1.1740744 (Griego 2026 [G2026]). Held (verified): C_3a > 1.1781 via exact big-integer certificate (tight rational 11781/10000 at den 10000); computed θ = 1.1781229473 at (m,T)=(180,340).

## Progress log
- R17: VERIFIED held raised 1.1779 → 1.1781 (the (180,340) tick R16 could not certify is now BANKED). The single load-bearing count R16 left unreproduced — s=|U+U| at m=180 (175 digits, head `95094840942995635699`, tail `45023204043252245774`) — was independently reproduced DIGIT-FOR-DIGIT (head AND tail, full 175-digit count) by the reviewer with a genuinely independent **memory-bounded** sumset DP: state (Sv, window-clamped bitmask of reachable Σy, the COMPLEMENTARY summand's digit-sum), distinct from both builder engines (count_sumset tracks Σx; ivl_sumset tracks Σx run-length). State count SATURATES at ≈70.9k (NOT exponential), peak RSS **46 MB** in this container — confirming the R16 "OOM at m≥130" was an environmental concurrency artifact, not an algorithmic wall. The reviewer engine was gate-validated vs from-scratch brute force on 27 cases (clamp-binding Σv≫T at m=5,T=22 / m=6,T=15 / m=7,T=16; non-contiguous Griego {0,2..10} AND {0,3,5,9}; contiguous {0..5} control), cross-checked vs a second totally-distinct frozenset-Minkowski engine, and reproduced the committed s at (110,210) digit-for-digit (107 digits) as a known-good anchor. d=|U−U| (218 digits, head `23513893541583867431`) re-derived via a minimal-pair (Σx*,Σy*) DP with the coordinatewise-min domination lemma re-verified, and M=max U (238 digits, head `49939369671774630152`) via EXACT low-to-high knapsack — both distinct from the builder's code, both brute-validated on 11 cases, both byte-exact (head AND tail) vs the committed scan row. Load-bearing cert re-derived with the reviewer's OWN raw big-integer powers from the INDEPENDENT integers: d^10000 > s^10000·(2M+1)^1781 ⟺ θ > 11781/10000 = 1.1781 HOLDS (log₁₀-margin **+54.61**, a genuine 54-order margin, not a rounding artifact), and the ^1782 power FAILS (**−183.39**, tight negative control). b=21 > 2·max(A)=20 ⇒ g injective + carry-free, so digit-vector counts equal |U±U| exactly; GHR2007 gives C_3a ≥ θ. Held: C_3a > 1.1781 > 1.1779 (prev) > 1.1740744 (record). New independent reproduction engine committed at `constants/3a/certificate/ivl_sumset.py` (builder) and reviewer engine at `/tmp/round-17/indep_bitmask.py`.
- R15: VERIFIED structural Lean advance (no held raise — held stays 1.1779). Sketch lean-c3a-def closed the two general spacing lemmas `setSum_tr_pair_disjoint` / `setDiff_tr_pair_disjoint` (B1a's cheap, cached-lemma-shaped half) SORRY-FREE. Reviewer reproduced `lake build C3a` EXIT 0 (2970 jobs); the two lemmas (lines 365/380) carry NO `sorry` warning, and an independent `#print axioms` (throwaway AxCheck) gives `[propext, Classical.choice, Quot.sound]` for BOTH — sorryAx-FREE. Statements re-derived by hand: `WithinDiam (B±B) lo diam ∧ diam < |c−c'| ⟹ Disjoint ((c+B)±B) ((c'+B)±B)` — a genuine diameter window AND a strict shift gap are required to conclude disjointness; correct and not over-strong. `griego_ak_disjoint` (B1a) is genuinely REASSEMBLED sorry-free from these two lemmas applied to the pieces of `an_separated` (lines 433–438, pure glue); its residual `sorryAx` traces ONLY to the documented `an_separated` hole (line 414) + the witness-data sorry defs (Ubase/Qbase/an_shift/an_index/an_interval), confirmed by `#print axioms`. No `axiom`/`native_decide`/`admit` smuggled (grep). Top-level `c3a_lower_bound_def` UNTOUCHED (still target θ>1.1771 < held 1.1779, still carries `sorryAx` via `griego_realizes` — no self-contained bound). Promoted `setSum_tr_pair_disjoint`/`setDiff_tr_pair_disjoint` to `lemmas/setsum-tr-pair-disjoint.md`. Remaining named hole on B1a: `an_separated` (the AP shift-spacing construction needing a `maxbk` element-range bound on `tpow Qbase Ubase n` + witness-data pinning). CHANGES REQUESTED (sketch stays live; ~11 holes remain).
- R14: VERIFIED structural Lean advance (no held raise — held stays 1.1779). Sketch lean-c3a-def closed sub-hole B1 (`griego_disjoint_union_count`) SORRY-FREE: reviewer re-ran `lake build C3a` EXIT 0 (2970 jobs) and confirmed via `#print axioms` that the 10 new finite-combinatorics lemmas (`setSum_card_decompose`/`setDiff_card_decompose` + supports `setSum/Diff_tr`, `setSum/Diff_tr_card`, `setSum/Diff_union`, `setSum/Diff_biUnion`) are all `[propext, Classical.choice, Quot.sound]` — sorryAx-FREE. B1's own `sorryAx` traces ONLY to documented holes it legitimately consumes (B1a `griego_ak_disjoint` + witness data Ubase/Qbase/Ubase_carryfree/an_interval/an_index/an_shift); no `axiom`/`native_decide`/`admit` in the file. The decompose lemmas faithfully state the GHR disjoint-union count `|(I ∪ ⋃ᵢ(aᵢ+B)) ± B| = mₙ·|B±B| + |I±B|` with BOTH disjointness hypotheses present and load-bearing (not assumed away); B1 wires them to the cached axiom-clean `tensor_pow_sumset_card`/`tensor_pow_diffset_card` (`|Bₙ±Bₙ|=|U±U|^(n+1)`, exponent reshape `^n→^(n+1)` verified correct against the `tpow` convention) and pins `bk n=tpow Qbase Ubase n`, `ak n=an_interval n ∪ ⋃ tr(an_shift n i)(bk n)`. The `∃ mn tsum tdiff` shape keeps the SAME multiplier `mn` on both sum/diff sides (the load-bearing structural fact B3 needs), deferring `tsum=tdiff` to B3 — a legal reshape, still feeds the chain. Top-level `c3a_lower_bound_def` UNTOUCHED, still has its documented holes (does NOT claim a self-contained C_3a bound). Promoted `setSum_card_decompose`/`setDiff_card_decompose` (+ 8 supports) to `lemmas/`. New named hole B1a exposed; B2/B3/B4 + witness data remain. CHANGES REQUESTED (sketch stays live).
- R13: VERIFIED held raised 1.1776 → 1.1779 (RECOVERY review of the unreviewed R12 build of griego-ntt-push). Same Griego family A={0,2,…,10}, b=21 pushed to the new point (m,T)=(170,321). Reviewer INDEPENDENTLY re-derived (s,d,M) from scratch with code (`/tmp/round-13/indep_verify.py` + `/tmp/round-13/diff2.py`) sharing NO path with the builder: s=|U+U| (166 digits, head 14558078951161095656) via a slack-mask shift-DOWN bitmask sumset DP (state = (Σz, mask of reachable Σx), distinct from the builder's absolute-Σx shift-UP bitmask), d=|U−U| (206 digits, head 14261126710543722641) via a minimal-pair (Σx*,Σy*) DP whose coordinatewise-min domination lemma the reviewer verified holds for every difference value w (not the builder's prebuilt table), M=max U (225 digits, head 29939883706029187284) via an EXACT low-to-high knapsack DP over budget — all three match the committed scan row digit-for-digit (byte-exact). Both counters validated vs from-scratch brute force on small cases (incl. clamp-binding Σv≫T at m=5,T=22 / m=6,T=15 / m=6,T=9 / m=5,T=7 / m=4,T=6 / m=5,T=12 and the non-contiguous Griego alphabet + a contiguous control {0..4}). Load-bearing cert re-derived with the reviewer's own exact big-int powers from the INDEPENDENT integers: d^10000 > s^10000·(2M+1)^1779 ⟺ θ > 11779/10000 = 1.1779 HOLDS (log-margin +73.03, not a rounding artifact), and ^1780 FAILS (−444.54, tight negative control); ^1778 PASS (+590.60). b=21 > 2·max(A)=20 ⇒ g injective + carry-free, digit-vector counts equal |U±U|; GHR2007 gives C_3a ≥ θ. Held: C_3a > 1.1779 > 1.1776 (prev) > 1.1740744 (record). Separately, the Lean sketch lean-c3a-def replaced the opaque C3aRealDef with a CONCRETE sSup{c | Realizes c} and restored the bounded-doubling clause (i) |A+B| ≤ K·|A|; reviewer ruled the definition a FAITHFUL lower-bound under-estimate (C3aRealDef ≤ registry C_3a — every Realizes-witness is a registry witness with the implicit constant c(K)=1), lake build C3a EXIT 0 with exactly 3 documented sorry (realizes_one / realizableSet_bddAbove / griego_realizes), no smuggled axiom (sorryAx traces only to those holes). It does NOT raise held (3 holes remain); definitional de-risking only — CHANGES REQUESTED.
- R11: VERIFIED held raised 1.1774 → 1.1776 (RECOVERY review of the unreviewed R9 build of griego-ntt-push). Same Griego family A={0,2,..,10}, b=21 pushed to the new point (m,T)=(160,304). Reviewer INDEPENDENTLY re-derived (s,d,M) from scratch with code (`/tmp/round-11/indep_verify.py`) sharing NO path with the builder: s=|U+U| (156 digits, head 62929132753659329051) via a slack-mask shift-DOWN bitmask DP (state = (Σv, mask of slack T−Σx), shift mask DOWN by x, prune slack > 2T−Σv — distinct from the builder's absolute-Σx shift-UP bitmask), d=|U−U| (194 digits, head 24477807455508710290) via a 2D minimal-pair (Σx*, Σy*) DP, M=max U (212 digits, head 17949698649023776230) via an EXACT low-to-high knapsack DP over budget (not the builder's greedy) — all three match the committed scan row digit-for-digit (byte-exact). Both counters validated vs from-scratch brute force on 11 small cases (incl. clamp-binding Σv≫T at m=5,T=22 / m=6,T=15 / m=6,T=9 and the non-contiguous Griego alphabet + a contiguous control). Load-bearing cert re-derived with the reviewer's own exact big-int powers from the INDEPENDENT integers: d^10000 > s^10000·(2M+1)^1776 ⟺ θ > 11776/10000 = 1.1776 HOLDS (log-margin +408.4, not a rounding artifact), and ^1777 FAILS (−78.7, tight negative control). b=21 > 2·max(A)=20 ⇒ g injective + carry-free, digit-vector counts equal |U±U|; GHR2007 gives C_3a ≥ θ. Held: C_3a > 1.1776 > 1.1774 (prev) > 1.1740744 (record). Separately, the Lean sketch lean-tensor-multiplicativity closed all 7 of its holes (lake build C3a EXIT 0, 2969 jobs; #print axioms on all 7 lemmas = [propext, Classical.choice, Quot.sound], NO sorryAx) — formalizing the FINITE half of the GHR bridge (carry-free tensor-power multiplicativity |U^⊗(k+1) ± U^⊗(k+1)| = |U±U|^(k+1)). It does NOT define C_3a and does NOT close the sup-realization limit, so it does NOT raise held; structural sub-lemma only.
- R8: VERIFIED held raised 1.1771 → 1.1774. Same Griego family A={0,2,..,10}, b=21 pushed to the new point (m,T)=(150,285). Reviewer INDEPENDENTLY re-derived (s,d,M) from scratch with code sharing no path with the builder: s=|U+U| (146 digits, head 96715660603655210677) via a slack-mask shift-DOWN bitmask DP (state = (Σv, mask of slack T−Σx), distinct from the builder's absolute-Σx shift-UP bitmask), d=|U−U| (182 digits, head 14968720620716469085) via a 2D minimal-pair DP, M=max U (199 digits, head 10761287009471054249) via a low-to-high DP (not greedy) — all three match the committed scan row digit-for-digit. Both counters validated vs from-scratch brute force on 18 small cases (incl. clamp-binding m=5,T=7 / m=4,T=6 / m=6,T=9 where Σv≫T, and the non-contiguous Griego alphabet + a contiguous control). Load-bearing cert re-derived with the reviewer's own exact big-int powers: d^10000 > s^10000·(2M+1)^1774 ⟺ θ > 11774/10000 = 1.1774 HOLDS (log-margin +125.1, not a rounding artifact), and ^1775 FAILS (−331.6, tight negative control). b=21 > 2·max(A)=20 ⇒ g injective + carry-free, digit-vector counts equal |U±U|; GHR2007 gives C_3a ≥ θ. Held: C_3a > 1.1774 > 1.1771 (prev) > 1.1740744 (record). Separately, the Lean sketch lean-native-decide-smallmt bumped its native_decide integer core to the den=10000 tight rational at (140,265): D^10000 > S^10000·Q^1771 (θ>1.1771) machine-checked HOLE-FREE (lake build EXIT 0, #print axioms shows only native_decide + Mathlib std, NO sorryAx) with a tightness control ¬(…^1772); but its bridge ghr:θ≤C3aReal to C_3a remains an explicit ASSUMED hypothesis (C3aReal opaque), so it stays minimally-verified (*) and does NOT raise held.
- R7: VERIFIED held raised 1.176 → 1.1771. Same Griego family A={0,2,..,10}, b=21 pushed to (m,T)=(140,265). Reviewer INDEPENDENTLY re-derived (s,d,M) from scratch with code sharing no path with the builder: s=|U+U| (136 digits) via a slack-based shift-DOWN bitmask DP (distinct formulation from the builder's absolute-sum_x bitmask), d=|U−U| (169 digits) via a 2D minimal-pair DP, M=max U (185 digits) via greedy validated vs exact DP — all three match the committed counts digit-for-digit. Both counters validated vs from-scratch brute force on 13 small cases (incl. clamp-binding m=5,T=22 / m=6,T=15 and non-contiguous alphabet) and vs the builder DP at m=20–50. Load-bearing cert re-derived with the reviewer's own big-int powers: d^10000 > s^10000·(2M+1)^1771 ⟺ θ > 11771/10000 = 1.1771 HOLDS (log-margin 159.3, not a rounding artifact), and ^1772 FAILS (−266.97, tight negative control). b=21 > 2·max(A)=20 ⇒ g injective + carry-free, digit-vector counts equal |U±U|; GHR2007 gives C_3a ≥ θ. Held: C_3a > 1.1771 > 1.176 (prev) > 1.1740744 (record).
- R4: VERIFIED held tightened 1.1741 → 1.176. Same (110,210) point, finer certificate rational: the exact big-integer inequality d^10000 > s^10000·(2M+1)^1760 ⟺ θ > 11760/10000 = 1.176 HOLDS, and ^1761 FAILS (so 1.176 is the tight k/10000, negative control). Reviewer independently re-derived (s,d,M) at (110,210) via a from-scratch bitmask DP validated vs brute force on 10 cases (incl. clamp/non-contiguous); s (107 digits), d (133 digits), M match the builder's values exactly; both inequality legs reproduced. Held: C_3a > 1.176 > 1.1740744. Separately, a Lean sketch (lean-native-decide-smallmt) machine-checks (lake build + native_decide, no sorryAx) the integer core D^40 > S^40·Q^7 (θ > 47/40 = 1.175) at (100,190) — literals independently matched — but its bridge from that integer fact to C_3a is an assumed (cited GHR2007) hypothesis with opaque Cnum, so it is a minimally-verified (*) Lean cert, NOT a self-contained Lean proof of C_3a > 1.175; it does not raise held on its own.
- R3: VERIFIED record beat. Griego family A={0,2,..,10}, b=21, U={Σ x_i b^i : x∈A^m, Σx_i≤T}. Exact column DP for (|U+U|,|U−U|,max U) — independently cross-checked from-scratch against brute force on 12 cases (incl. clamp-exercising m=6,T=15 / m=4,T=9 / m=5,T=22). Load-bearing certificate is the pure big-integer inequality d^10000 > s^10000·(2M+1)^1741 ⟺ θ > 1.1741 > 1.1740744; reproduced independently with log-margin 6381.8 at (110,210). b=21>2·max(A)=20 makes g injective + carry-free, so digit-vector counts equal |U±U| exactly; GHR2007 gives C_3a ≥ θ. Anchor (80,150)=1.1740744477 reproduces Griego's record; first beat at (80,154)=1.1741714; best (110,210)=1.1760056. Negative control (80,148)=1.17393 correctly fails. Held: C_3a > 1.1741.
