# R27 explore triage — 82a UPPER (read-only, fast)

Held verified upper = **0.2538893183** (j3/j9 Doche A-base, verify_upper_q8A.py), UNCHANGED since R4.
On-disk state git-verified bit-identical since R10: newest non-triage artifact is still
`certificate/verify_dual_loci_decomposition.py` (commit 239b19b); every certificate-dir commit since is
that R10 decomposition, and only R11–R26 triage docs + R11-outline.md committed afterward. Milestone
count = 19 (eval grep), matching run_state. No new user idea — the round-27 prompt is bit-for-bit the
R1 BMQS Thm 4.5 density / R6 integer-transfinite-diameter thesis / R7 "minimize r_Q over Z[X]" design
principle (all three user messages timestamped 2026-06-11, already recorded as Goal Updates R1/R6/R7 and
refuted R12–R26).

## Q1 — any NEW WITD-independent computed artifact a builder could bank in one round?  **DRY**
Both Doche branches are reviewer-verified MAXIMAL FIRING SETS (A-base R7: 73845 originals -> 0 firing;
B-branch R8: all 263 admissible atoms above the firing gate). R10 banked the one clean fixed-q numerator
identity; the R9 unified first-variation theorem covers every derivative direction, so no second R10-style
identity remains. The only un-computed "new number" — logarithmic capacity / transfinite diameter /
equilibrium measure of ACT(A)/ACT(B) — is Q-INDEPENDENT set data; tying it to the verified Q-weighted
marginals r_tilde/m_B IS the open WITD equality inf_Q r_tilde = -log d_inf, which R7 PROVED FAILS in the
load-bearing direction (integer roots sit strictly inside the unit disk, cannot reach the deepest U^nu
wells; integer inf strictly ABOVE continuous -log d_inf). It backs nothing and the partition is
anchor-dependent (|ACT(A)| = 0.428 held vs 0.0686 R10 convention — not one well-defined number). DRY.

## Q2 — BMQS Thm 4.5 density: any non-Doche mu_{P,Q} upper construction?  **DRY**
Exact dichotomy (R1, re-attacked R12–R26): a non-Doche mu_{P,Q} object is either a bare single pair
(value 0.34–0.69, hopeless) or the weighted/multi-pair recast, which IS the saturated Doche V = prod P^m
family. BMQS Thm 6.5 strong duality pins the primal optimum to that saturated cone. The density LIMIT needs
high-degree minimal-poly sequences with no efficient one-round compass (BMQS §1.7 wall) and still hits R7's
geometric obstruction. Multi-round gamble, not a one-round feasible upper construction. DRY.

## Q3 — "minimize r_Q over Z[X]" (R7 design principle): anything not already closed?  **DRY**
This IS the WITD primal from the construction side. Fully closed by both maximal-firing-set theorems:
A-base (R7, 73845 originals -> 0 firing; cheapest coprime original factor w^2-w+1 has r_tilde = +0.01311 >
best reachable firing margin) and B-branch (R8, all 263 admissible atoms have int/deg >= 0.2631 > firing
threshold 0.2539). No reachable original block fires. DRY.

## Adversarial self-check on the DRY verdict
Could a builder produce a NEW script/identity the reviewer would bank? The reviewer banks only genuinely
new CHECKABLE content that means something (not prose re-assembly, not re-runs). The single un-computed
quantity (locus capacity) collapses to the refuted WITD equality and backs nothing. No new user idea has
arrived. Decision holds.

## Overall verdict: DRY (17th consecutive). No valid builder dispatch.
82a UPPER is PUBLICATION-COMPLETE (R4 record 0.2538893183 + both-branch saturation R7/R8 + full
first-variation engine R6/R8/R9 + R10 dual-loci decomposition). Recommend pivot to write-up, the LOWER
side (separate dispatch — forbidden here by user R6 directive), or a different registry constant. Only a
qualitatively new (non-Doche) user idea, or a technique for the open multi-round WITD equality, unlocks
further UPPER work.
